using System;

//If Statement
class Program
{
    public static void Main()
    {
        // If statement pseudocode
        //if(statement1 && statement2) { }    && checks only first statement if first statement is false (faster than &)
        //if(statement1 & statement2) { }     &  checks both statement in every situation 
        //if(statement1 || statement2) { }    || checks only first statement if first statement is true(faster than |)
        //if(statement1 | statement2) { }     |  checks both statement in every situation 
    }
}